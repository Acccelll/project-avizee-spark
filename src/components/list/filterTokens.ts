/**
 * Standard widths for filter controls (MultiSelect, Select, DateInput) used
 * across list/grid screens. Use these tokens instead of hardcoding `w-[Xpx]`
 * so filter controls stay visually consistent across modules.
 */
export const FILTER_W_SM = "w-full sm:w-[140px]";
export const FILTER_W_MD = "w-full sm:w-[180px]";
export const FILTER_W_LG = "w-full sm:w-[220px]";
export const FILTER_W_XL = "w-full sm:w-[260px]";

/** Date input width, slightly larger to fit a full date string comfortably. */
export const FILTER_W_DATE = "w-full sm:w-[150px]";
